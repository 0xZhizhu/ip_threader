/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configuration options go here
  // For example, if you had specific experimental flags or other settings, re-add them here.
  // experimental: {
  //   serverComponentsExternalPackages: ['@campnetwork/origin'],
  // },
};

module.exports = nextConfig;